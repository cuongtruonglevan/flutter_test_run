import 'package:flutter/material.dart';

class AppColors {
  /// This color is the background color for major parts of the app
  static const Color primaryColor = Color(0xFF213e7c);

  /// This color is used to display contents laying on primaryColor background
  static const Color onPrimaryColor = Colors.white;

  /// This color is used to call attention to parts of the app
  static const Color secondaryColor = Color(0xFF449CED);

  /// This color is used to display contents laying on secondaryColor background
  static const Color onSecondaryColor = Colors.white;

  /// This color is used to show lines(divider, border)
  static const Color lineColor = Color(0xFFEAEEF6);

  /// This color is used to show default texts
  static const Color textDefaultColor = Color(0xFF6F6F87);
}
